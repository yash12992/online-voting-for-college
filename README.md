# online-voting-system
This system enables registered users to log in, view available candidates, and cast their vote electronically. The application ensures that each user can vote only once and that all votes are securely stored in the database. It also provides an admin panel to manage candidates, voters, and election results.
